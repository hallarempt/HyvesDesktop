# Generate an application bundle (new-style)
SET(CPACK_BUNDLE_ICON "${CMAKE_SOURCE_DIR}/distribution/osx/kwekker.icns")
SET(CPACK_BUNDLE_NAME "HyvesDesktop")
SET(CPACK_BUNDLE_PLIST "${CMAKE_CURRENT_BINARY_DIR}/Info.plist")
SET(CPACK_CMAKE_GENERATOR "${CMAKE_GENERATOR}")
SET(CPACK_GENERATOR "Bundle")
SET(CPACK_INSTALL_CMAKE_PROJECTS "${HD_CPACK_INSTALL_CMAKE_PROJECTS}")
SET(CPACK_PACKAGE_FILE_NAME "Hyves-Desktop-setup-${HD_VERSION}")
SET(CPACK_PACKAGE_ICON "${CMAKE_SOURCE_DIR}/distribution/osx/volume.icns")
SET(CPACK_PACKAGE_NAME "${HD_CPACK_PACKAGE_NAME}")
SET(CPACK_PACKAGE_VERSION "${HD_CPACK_PACKAGE_VERSION}")
SET(CPACK_PACKAGE_DESCRIPTION "${HD_CPACK_PACKAGE_DESCRIPTION}")
SET(CPACK_PACKAGE_DESCRIPTION "${HD_CPACK_PACKAGE_DESCRIPTION}")

CONFIGURE_FILE(${CMAKE_CURRENT_SOURCE_DIR}/bundle.sh.in ${CMAKE_CURRENT_BINARY_DIR}/bundle.sh @ONLY)
CONFIGURE_FILE(${CMAKE_CURRENT_SOURCE_DIR}/Info.plist.in ${CMAKE_CURRENT_BINARY_DIR}/Info.plist @ONLY)
	
