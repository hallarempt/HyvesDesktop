Windows updating:

* start the installer & close the running app

OS X updating: 

* use sparkle

Linux updating:

* run the installer script with the prefix of the running installer & then quit and restart


XXX: Refactor into per-platform code subdirs.